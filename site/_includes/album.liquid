<a href="/photos/{{ path }}">
	<figure id="{{ path }}">
		<img
			alt="{{ title }}"
			src="https://cdn.m0b1.dev/{{ image }}?class=thumbnail"
			loading="{{ loading | default: 'lazy' }}"
			width="640"
			height="360"
		/>
		<figcaption>
			{{ title }} &rArr;
		</figcaption>
	</figure>
</a>
